const initialValues = {
  users: {
    name: '',
    email: '',
    phonenumber: '',
    password: '',
    confirmpassword: '',
  },
};

const reducerFunction = (state = initialValues, action) => {
  return state;
};

export default reducerFunction;
